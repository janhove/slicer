% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring.R
\name{nlpd_gpr}
\alias{nlpd_gpr}
\title{Negative log predictive density for Gaussian process regression models}
\usage{
nlpd_gpr(fit, y_test, add_noise = TRUE, nugget = 1e-10)
}
\arguments{
\item{fit}{A Gaussian process model fit obtained by \code{\link[=gpr_predict]{gpr_predict()}} or
\code{\link[=fit_gpr]{fit_gpr()}}.}

\item{y_test}{A vector of the true test outcomes.}

\item{add_noise}{If \code{TRUE} (default), \code{y_test} is assumed to be noisy
and the observation noise is added to the variance in the posterior
covariance.}

\item{nugget}{Numeric; base jitter added to the covariance diagonal if needed
to ensure positive definiteness. Increased multiplicatively if required.}
}
\value{
A scalar giving the average NLPD. The returned value has an attribute
\code{"jitter_used"} indicating the final diagonal jitter added (0 if none).
}
\description{
Computes the average negative log predictive density (NLPD) of test outcomes
under the posterior Gaussian distribution returned by a GPR model. If the
posterior covariance is not positive definite, a small diagonal "nugget" is
added adaptively for numerical stability.
}
\examples{
set.seed(2026-06-25)
N1 <- 50
N2 <- 20
x_train <- seq(-pi, pi, length.out = N1)
x_test  <- runif(N2, -pi, pi)
y_train <- x_train * plogis(x_train) * cos(x_train) + rnorm(N1, sd = 0.5)
y_test <- x_test * plogis(x_test) * cos(x_test)
D2 <- outer(c(x_train, x_test), c(x_train, x_test), "-")^2
fit <- fit_gpr(D2, seq_len(N1), N1 + seq_len(N2), y_train, y_test,
               runs = 20L, cores = 1)
# noisy outcome
nlpd_gpr(fit, y_test + rnorm(N2, sd = 0.5))
# clean outcome
nlpd_gpr(fit, y_test, add_noise = FALSE)
}
